# Core
torch>=2.0,<3.0
numpy>=1.24,<3.0
tqdm>=4.60

# Model loading
transformer_lens>=2.0,<3.0
sae-lens>=4.0,<6.0

# Data
datasets>=2.14
huggingface_hub>=0.20

# LLM annotation (via OpenRouter, OpenAI-compatible API)
openai>=1.0

# Local annotation (install separately: pip install vllm)
# vllm>=0.6.0
