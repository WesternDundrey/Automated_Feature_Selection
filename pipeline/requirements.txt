# Core
torch>=2.6
numpy>=1.26
tqdm>=4.66

# Model loading
transformer_lens>=2.8
sae-lens>=5.0

# Data
datasets>=3.0
huggingface_hub>=0.27

# LLM annotation (via OpenRouter, OpenAI-compatible API)
openai>=1.60

# Transformers + vLLM (let uv resolve compatible versions)
transformers>=4.56
vllm>=0.18
