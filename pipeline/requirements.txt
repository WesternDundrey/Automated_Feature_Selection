# vLLM (pulls torch, numpy, transformers automatically)
vllm>=0.18

# Data
datasets>=3.0
huggingface_hub>=0.27

# LLM annotation (via OpenRouter)
openai>=1.60

# Progress bars
tqdm>=4.66

# sae-lens + transformer-lens: install separately after the above.
# They pin numpy<2 but work fine with numpy 2.x at runtime.
#   pip install sae-lens transformer-lens --no-deps
